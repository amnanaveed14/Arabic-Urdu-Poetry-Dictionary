# H2 In-Memory Test Database
# This mimics MySQL but runs inside memory. 
# It is deleted automatically when the test finishes.
db.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=MySQL
db.user=sa
db.password=
db.driver=org.h2.Driver